# ── Relay environment ─────────────────────────────────────────────
# AFTER DOWNLOAD: rename this to data/captain.work.md (work) or
# data/captain.personal.md (home). Those names are gitignored — your
# secrets never reach git. NEVER commit a filled-in copy.

# ── profile: picks board adapter + notification channel ──
RELAY_PROFILE=personal             # personal | work

# ── personal (home): GitHub via gh CLI + Telegram ──
# No GitHub token needed — Relay uses your existing `gh auth login`.
# Just tell it which repo:
GITHUB_REPO=                       # e.g. ManikantaR/MoneyPulse
TELEGRAM_BOT_TOKEN=                # for notifications
TELEGRAM_CHAT_ID=

# ── work: TFS (Team Foundation Server / Azure DevOps Server) + Teams ──
# Relay does NOT handle your PAT — your existing PowerShell scripts read it
# from their own configured env. Relay just calls those scripts.
TFS_URL=                           # your collection URL, e.g. https://tfs.company.com/tfs/DefaultCollection
RELAY_AREA=                        # TFS area path that fences pickup
RELAY_TFS_SCRIPTS=./tfs-scripts    # path to YOUR existing PS scripts
TEAMS_WEBHOOK_URL=                 # Teams incoming webhook

# ── tuning (safe defaults) ──
RELAY_HARNESS=claude               # claude | codex | opencode
RELAY_PROBE_INTERVAL=300
RELAY_HANG_THRESHOLD=900
RELAY_POLL=15
